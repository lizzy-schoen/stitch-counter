{
  "name": "knit-counter",
  "version": "1.0.0",
  "description": "Alexa skill for tracking knitting rows with reminders",
  "main": "index.js",
  "scripts": {
    "test": "jest --coverage"
  },
  "dependencies": {
    "ask-sdk": "^2.14.0",
    "ask-sdk-dynamodb-persistence-adapter": "^2.14.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
